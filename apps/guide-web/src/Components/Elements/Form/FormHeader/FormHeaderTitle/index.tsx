import React from 'react';

const FormHeaderTitle: React.FC = ({ children }) => {
  return <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900">{children}</h2>;
};

export default FormHeaderTitle;
